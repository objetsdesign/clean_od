<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_catalog_sync_auto_imported_stock" model="ir.cron">
        <field name="name">Catalogue : synchroniser le stock des produits Odoo importés</field>
        <field name="model_id" ref="model_catalog_product"/>
        <field name="state">code</field>
        <field name="code">model._cron_sync_auto_imported_stock()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>

</odoo>
