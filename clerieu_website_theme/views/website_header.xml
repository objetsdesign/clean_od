<odoo>

    <record id="template_header_search" model="ir.ui.view">
        <field name="name">Clerieu - Header personnalisé</field>
        <field name="inherit_id" ref="website.template_header_search"/>
        <field name="active" eval="True"/>
        <field name="website_id" eval="2"/>
        <field name="arch" type="xml">
        <xpath expr="//t[@t-call='website.navbar']" position="replace">
            <t t-call="website.navbar">
                <t t-set="_navbar_classes" t-valuef="d-none d-lg-block p-0"/>

                <div id="o_main_nav" class="o_main_nav flex-wrap" style="background: transparent;">
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&amp;display=swap"/>
                    <!-- Top bar caché -->
                    <div aria-label="Haut" class="o_header_hide_on_scroll d-none">
                        <div class="container d-flex justify-content-between flex-wrap w-100">
                            <ul class="o_header_search_left_col navbar-nav flex-wrap">
                                <t t-call="website.placeholder_header_language_selector">
                                    <t t-set="_div_classes" t-valuef="h-100 border-end o_border_contrast"/>
                                    <t t-set="_btn_class" t-valuef="o_navlink_background_hover btn btn-sm d-flex align-items-center h-100 rounded-0 p-2 text-reset"/>
                                </t>
                                <t t-call="website.placeholder_header_text_element">
                                    <t t-set="_txt_elt_content" t-valuef="sentence"/>
                                    <t t-set="_div_class" t-valuef="d-flex align-items-center h-100 border-end p-2 text-muted o_border_contrast"/>
                                </t>
                            </ul>
                            <ul class="navbar-nav flex-wrap">
                                <t t-call="website.placeholder_header_social_links">
                                    <t t-set="_div_class" t-valuef="d-flex align-items-center h-100 border-start px-1 o_border_contrast"/>
                                </t>
                                <t t-call="portal.placeholder_user_sign_in">
                                    <t t-set="_link_class" t-valuef="o_navlink_background_hover btn btn-sm d-flex align-items-center h-100 rounded-0 border-0 border-start px-3 text-reset o_border_contrast"/>
                                </t>
                                <t t-call="portal.user_dropdown">
                                    <t t-set="_user_name" t-value="true"/>
                                    <t t-set="_item_class" t-valuef="dropdown border-start o_border_contrast"/>
                                    <t t-set="_link_class" t-valuef="o_navlink_background_hover btn btn-sm d-flex align-items-center h-100 rounded-0 p-2 text-reset"/>
                                    <t t-set="_dropdown_menu_class" t-valuef="dropdown-menu-end"/>
                                </t>
                            </ul>
                        </div>
                    </div>

                    <!-- Barre principale -->
                    <div aria-label="Bas" class="container d-grid align-items-center w-100 o_grid_header_3_cols" style="padding-top: 18px; padding-bottom: 18px;">

                        <!-- Menu gauche -->
                        <t t-call="website.navbar_nav">
                            <t t-set="_nav_class" t-valuef="me-4"/>
                            <t t-foreach="website.menu_id.child_id" t-as="submenu">
                                <t t-call="website.submenu">
                                    <t t-set="item_class" t-valuef="nav-item"/>
                                    <t t-set="link_class" t-valuef="nav-link"/>
                                </t>
                            </t>
                        </t>

                        <!-- Logo centre -->
                        <div class="custom_center_text mx-auto" style="position:relative;">
                            <a href="/" class="header-logo-link" style="position: relative; display: flex; align-items: center; justify-content: center;">
                                <img src="/web/image/2388-615b2bc4/logo.webp" alt="" class="img img-fluid o_we_custom_image header-logo-img" data-mimetype="image/webp" data-original-id="2387" data-original-src="/web/image/2387-64104fec/logo.png" data-mimetype-before-conversion="image/png" data-resize-width="NaN" loading="lazy"/>
                            </a>
                        </div>

                        <!-- Icônes droite -->
                        <ul class="o_header_search_right_col navbar-nav align-items-center gap-3 ms-auto ps-3">

                            <!-- Search -->
                            <li class="nav-item">
                                <a href="/shop?search=" class="nav-link p-0">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
                                        <circle cx="11" cy="11" r="7"/>
                                        <line x1="16.5" y1="16.5" x2="22" y2="22"/>
                                    </svg>
                                </a>
                            </li>

<!-- Wishlist -->
<li class="nav-item" id="gaia_wishlist_item" style="position:relative;">
    <a href="/shop/wishlist" class="nav-link p-0">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
        </svg>
    </a>
</li>
<script>
(function() {
    function initBadges() {

        /* ── PANIER ── */
        var cartLink = document.querySelector('li.nav-item a[href="/shop/cart"]');
        if (cartLink) {
            var cartLi = cartLink.closest('li');
            cartLi.style.position = 'relative';
            var cartBadge = document.createElement('span');
            cartBadge.id = 'gaia_cart_badge';
            cartBadge.style.cssText = 'position:absolute;top:0px;right:-4px;background:#e63946;color:#fff;border-radius:50%;min-width:16px;height:16px;font-size:9px;font-weight:700;display:none;align-items:center;justify-content:center;font-family:Poppins,sans-serif;line-height:1;padding:0 3px;z-index:99;pointer-events:none;';
            cartLi.appendChild(cartBadge);

            function updateCart(qty) {
                qty = parseInt(qty) || 0;
                cartBadge.textContent = qty;
                cartBadge.style.display = qty &gt; 0 ? 'flex' : 'none';
            }

            var domQty = document.querySelector('.my_cart_quantity');
            if (domQty) {
                updateCart(domQty.textContent.trim());
            } else {
                var xhr = new XMLHttpRequest();
                xhr.open('POST', '/web/dataset/call_kw', true);
                xhr.setRequestHeader('Content-Type', 'application/json');
                xhr.onload = function() {
                    try {
                        var data = JSON.parse(xhr.responseText);
                        if (typeof data.result === 'number') updateCart(data.result);
                    } catch(e) {}
                };
                xhr.send(JSON.stringify({
                    jsonrpc: '2.0',
                    method: 'call',
                    id: 1,
                    params: {
                        model: 'sale.order',
                        method: 'search_count',
                        args: [[['state', '=', 'draft'], ['website_id', '!=', false]]],
                        kwargs: { context: {} }
                    }
                }));
            }

            var origOpen = XMLHttpRequest.prototype.open;
            XMLHttpRequest.prototype.open = function(method, url) {
                this.addEventListener('load', function() {
                    try {
                        var json = JSON.parse(this.responseText);
                        var qty = json &amp;&amp; json.result &amp;&amp; json.result.cart_quantity;
                        if (qty !== undefined) updateCart(qty);
                    } catch(e) {}
                });
                origOpen.apply(this, arguments);
            };
        }

        /* ── WISHLIST ── */
        var wishlistLi = document.getElementById('gaia_wishlist_item');
        if (wishlistLi) {
            var wBadge = document.createElement('span');
            wBadge.id = 'gaia_wishlist_badge';
            wBadge.style.cssText = 'position:absolute;top:-6px;right:-8px;background:#e8372d;color:#fff;border-radius:8px;min-width:16px;height:16px;font-size:10px;font-weight:500;display:none;align-items:center;justify-content:center;font-family:Poppins,sans-serif;line-height:1;padding:0 3px;z-index:99;pointer-events:none;';
            wishlistLi.appendChild(wBadge);

            function updateWishlist(qty) {
                qty = parseInt(qty) || 0;
                wBadge.textContent = qty &gt; 99 ? '99+' : qty;
                wBadge.style.display = qty &gt; 0 ? 'flex' : 'none';
            }

            function fetchWishlist() {
                var xhr = new XMLHttpRequest();
                xhr.open('POST', '/web/dataset/call_kw', true);
                xhr.setRequestHeader('Content-Type', 'application/json');
                xhr.onload = function() {
                    try {
                        var data = JSON.parse(xhr.responseText);
                        if (typeof data.result === 'number') updateWishlist(data.result);
                    } catch(e) {}
                };
                xhr.send(JSON.stringify({
                    jsonrpc: '2.0',
                    method: 'call',
                    id: 2,
                    params: {
                        model: 'product.wishlist',
                        method: 'search_count',
                        args: [[]],
                        kwargs: { context: {} }
                    }
                }));
            }

            fetchWishlist();

            document.addEventListener('click', function(e) {
                var btn = e.target.closest('[data-action="add_to_wishlist"], .o_add_wishlist, .o_wishlist_btn');
                if (btn) setTimeout(fetchWishlist, 800);
            });
        }
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initBadges);
    } else {
        initBadges();
    }
})();
</script>

                            <!-- Panier -->
                            <li class="nav-item">
                                <a href="/shop/cart" class="nav-link p-0">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="22" height="23" viewBox="0 0 28 29" fill="none">
                                        <path d="M16.7506 3.75C16.7506 2.95435 16.4346 2.19129 15.8722 1.62868C15.3097 1.06607 14.5469 0.75 13.7515 0.75C12.9561 0.75 12.1933 1.06607 11.6308 1.62868C11.0684 2.19129 10.7524 2.95435 10.7524 3.75M24.6383 10.794L26.7152 24.294C26.7808 24.7213 26.7534 25.1577 26.6348 25.5734C26.5162 25.9891 26.3092 26.3742 26.0279 26.7025C25.7466 27.0307 25.3978 27.2942 25.0052 27.475C24.6127 27.6558 24.1857 27.7496 23.7535 27.75H3.74947C3.31705 27.7501 2.88973 27.6566 2.4968 27.4759C2.10388 27.2953 1.75466 27.0319 1.47306 26.7036C1.19147 26.3753 0.984172 25.99 0.865382 25.5741C0.746593 25.1582 0.71912 24.7215 0.784848 24.294L2.86173 10.794C2.97068 10.0854 3.32971 9.43923 3.87382 8.97255C4.41793 8.50588 5.11112 8.24956 5.82785 8.25H21.6751C22.3916 8.24991 23.0845 8.5064 23.6282 8.97304C24.172 9.43968 24.5294 10.0856 24.6383 10.794Z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                                    </svg>
                                </a>
                            </li>

                            <!-- Compte -->
                            <t t-call="portal.user_dropdown">
                                <t t-set="_user_name" t-value="false"/>
                                <t t-set="_item_class" t-valuef="dropdown"/>
                                <t t-set="_link_class" t-valuef="o_navlink_background_hover btn p-0 border-0 bg-transparent d-flex align-items-center"/>
                                <t t-set="_dropdown_menu_class" t-valuef="dropdown-menu-end"/>
                            </t>

                            <style>
header .nav-link,
header .nav-link *,
header a,
header a * {
    font-family: 'Poppins', sans-serif !important;
}
/* =========================================
   RESET
========================================= */
#o_main_nav, .o_main_nav, header, header nav {
    box-shadow: none !important;
    border-bottom: none !important;
}

/* =========================================
   ANTI-DOUBLON HEADER (mobile notamment)
========================================= */
header ~ header,
#wrapwrap &gt; header + header,
body &gt; header + header {
    display: none !important;
}
header nav.navbar {
    box-shadow: rgba(0,0,0,0) 0px 0px 0px 0px !important;
}
#o_main_nav .container.d-grid {
    padding-top: 36px !important;
    padding-bottom: 28px !important;
}

/* =========================================
   NAV-LINK BASE
========================================= */
header .nav-link {
    font-family: 'Poppins', sans-serif;
    font-size: 0.82rem;
    font-weight: 400 !important;
    letter-spacing: 0.04em;
    text-transform: capitalize;
    text-decoration: none !important;
}

/* =========================================
   BACKGROUND
========================================= */
header:not(.o_header_affixed) #o_main_nav {
    background: transparent !important;
}
header.o_header_affixed #o_main_nav {
    background: white !important;
    transition: all .3s ease;
}

/* =========================================
   BORDURE BAS — AUTRES PAGES UNIQUEMENT
========================================= */
body:not(.o_homepage) header {
    border-bottom: 1px solid #2b2017 !important;
}
body.o_homepage header {
    border-bottom: none !important;
}

/* =========================================
   ACCUEIL — SUPERPOSITION FORCÉE
   Indépendant de l'option Odoo "En-tête en
   superposition".
========================================= */
body.o_homepage header:not(.o_header_affixed) {
    position: absolute !important;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1030;
    background: transparent !important;
}

/* Tentative : forcer la transparence sur la barre mobile native
   d'Odoo aussi (classes usuelles Bootstrap/Odoo pour ce bloc). */
body.o_homepage header:not(.o_header_affixed) nav,
body.o_homepage header:not(.o_header_affixed) .navbar,
body.o_homepage header:not(.o_header_affixed) .o_header_mobile,
body.o_homepage header:not(.o_header_affixed) [class*="navbar-"] {
    background: transparent !important;
    background-color: transparent !important;
}
body.o_homepage header:not(.o_header_affixed) .navbar-toggler,
body.o_homepage header:not(.o_header_affixed) .navbar-toggler-icon {
    filter: brightness(0) invert(1);
    border-color: rgba(255,255,255,.6) !important;
}

/* =========================================
   HOMEPAGE TOP = BLANC
========================================= */
#wrapwrap.o_header_overlay &gt; header:not(.o_header_affixed) .nav-link,
#wrapwrap.o_header_overlay &gt; header:not(.o_header_affixed) .nav-link.active,
#wrapwrap.o_header_overlay &gt; header:not(.o_header_affixed) .navbar .nav-item &gt; .nav-link,
#wrapwrap.o_header_overlay &gt; header:not(.o_header_affixed) svg,
body.o_homepage header:not(.o_header_affixed) .nav-link,
body.o_homepage header:not(.o_header_affixed) .nav-link.active,
body.o_homepage header:not(.o_header_affixed) .navbar .nav-item &gt; .nav-link,
body.o_homepage header:not(.o_header_affixed) svg {
    color: #ffffff !important;
    -webkit-text-fill-color: #ffffff !important;
    stroke: #ffffff !important;
}

/* =========================================
   AUTRES PAGES TOP = NOIR
========================================= */
body:not(.o_homepage) header:not(.o_header_affixed) .nav-link,
body:not(.o_homepage) header:not(.o_header_affixed) svg {
    color: #2b2017 !important;
    -webkit-text-fill-color: #2b2017 !important;
    stroke: #2b2017 !important;
}

/* =========================================
   SCROLL = NOIR PARTOUT
========================================= */
header.o_header_affixed .nav-link,
header.o_header_affixed svg {
    color: #2b2017 !important;
    -webkit-text-fill-color: #2b2017 !important;
    stroke: #2b2017 !important;
}

/* =========================================
   LOGO CENTRE — UNE SEULE IMAGE
   Beige en temps normal, forcé en noir au
   scroll (filtre CSS, indépendant du fichier
   image importé) — pareil sur toutes les pages.
========================================= */
.custom_center_text {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 220px;
}

.header-logo-link {
    text-decoration: none !important;
}

.header-logo-img {
    filter: none;
    transition: filter .3s ease;
}

/* Au scroll (header fixé) : logo forcé en noir, sur toutes les pages */
header.o_header_affixed .header-logo-img {
    filter: brightness(0) saturate(100%);
}

/* =========================================
   ICÔNE USER — CARET MASQUÉ
========================================= */
#o_main_nav .o_header_search_right_col .dropdown &gt; .btn .caret,
#o_main_nav .o_header_search_right_col .dropdown &gt; .btn .fa,
#o_main_nav .o_header_search_right_col .dropdown &gt; .btn b,
#o_main_nav .o_header_search_right_col .dropdown &gt; .btn::after {
    display: none !important;
}

/* BASE icône user (icône = par défaut du composant, ou filet JS) */
#o_main_nav .o_header_search_right_col .dropdown &gt; .btn {
    background-color: transparent !important;
    padding: 0 !important;
    border: none !important;
    box-shadow: none !important;
}

i.fa.fa-plus.position-relative.z-1 {
    font-size: 12px !important;
    font-weight: 200 !important;
}
i.position-relative.z-1.fa.fa-minus {
    font-size: 12px !important;
}

/* =========================================
   SUPPRIMER TRAIT HOMEPAGE
========================================= */
#wrapwrap.o_header_overlay &gt; header,
#wrapwrap.o_header_overlay &gt; header nav,
#wrapwrap.o_header_overlay &gt; header .navbar,
#wrapwrap.o_header_overlay &gt; header .navbar-light,
body.o_homepage header,
body.o_homepage header nav,
body.o_homepage header .navbar,
body.o_homepage header .navbar-light {
    border: none !important;
    border-bottom: none !important;
    box-shadow: none !important;
    outline: none !important;
}
/* =========================================
   MOBILE MENU = NOIR — PRIORITÉ MAXIMALE
========================================= */
#wrapwrap #top_menu_collapse_mobile .nav-link,
#wrapwrap #top_menu_collapse_mobile a,
#wrapwrap #top_menu_collapse_mobile .offcanvas-body .nav-link,
#wrapwrap #top_menu_collapse_mobile .offcanvas-body a,
#wrapwrap .offcanvas-end .nav-link,
#wrapwrap .offcanvas-end a,
#wrapwrap.o_header_overlay header #top_menu_collapse_mobile .nav-link,
#wrapwrap.o_header_overlay header #top_menu_collapse_mobile a {
    color: #2b2017 !important;
    -webkit-text-fill-color: #2b2017 !important;
    stroke: #2b2017 !important;
}
                            </style>
<script>
(function() {
    /* Filet de sécurité anti-doublon (v2) : au lieu de chercher des
       &lt;header&gt; en double, on cible directement les boutons "menu
       hamburger" mobile (identifiables via les attributs standards
       Bootstrap/Odoo), qui sont un marqueur fiable d'une barre mobile.
       S'il y en a plus d'un visible en haut de page, on ne garde que
       le tout premier bloc et on masque les suivants. */
    function dedupeMobileBars() {
        var togglers = document.querySelectorAll(
            '[data-bs-toggle="offcanvas"], .navbar-toggler, button[aria-label*="menu" i], button[aria-label*="Menu" i]'
        );
        if (togglers.length &lt;= 1) return;

        for (var i = 1; i &lt; togglers.length; i++) {
            var toggler = togglers[i];
            // Ne masquer que si c'est un doublon proche du haut de page
            // (barre mobile), pas un bouton hamburger utilisé ailleurs
            // (ex: filtre boutique).
            if (toggler.getBoundingClientRect().top &gt; 400) continue;

            // Remonte jusqu'à un conteneur de ligne raisonnable (header/nav/div)
            // sans remonter jusqu'au &lt;body&gt; entier.
            var row = toggler.closest('header, nav, .navbar, .o_main_nav, div');
            if (row &amp;&amp; row !== document.body) {
                row.style.display = 'none';
            } else {
                toggler.style.display = 'none';
            }
        }
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', dedupeMobileBars);
    } else {
        dedupeMobileBars();
    }
    window.addEventListener('load', dedupeMobileBars);
})();
</script>
<script>
(function() {
    /* Filet de sécurité anti-doublon : si un second &lt;header&gt;
       venait à exister dans le DOM, on ne garde que le premier. */
    function removeDuplicateHeaders() {
        var headers = document.querySelectorAll('#wrapwrap &gt; header, body &gt; header');
        for (var i = 1; i &lt; headers.length; i++) {
            headers[i].parentNode &amp;&amp; headers[i].parentNode.removeChild(headers[i]);
        }
    }
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', removeDuplicateHeaders);
    } else {
        removeDuplicateHeaders();
    }
})();
</script>
<script>
(function() {
    var PERSON_SVG = '&lt;svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"&gt;&lt;path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/&gt;&lt;circle cx="12" cy="7" r="4"/&gt;&lt;/svg&gt;';

    function ensureAccountIcon() {
        var toggle = document.querySelector('#o_main_nav .o_header_search_right_col .dropdown &gt; .btn, #o_main_nav .o_header_search_right_col .dropdown &gt; a.dropdown-toggle');
        if (!toggle) return;
        // Une icône existe déjà (svg/i/img par défaut, ou avatar connecté) : on ne touche à rien.
        if (toggle.children.length &gt; 0 || toggle.querySelector('svg, img, i')) return;
        // Rien d'affiché : on injecte une icône par défaut.
        toggle.insertAdjacentHTML('afterbegin', PERSON_SVG);
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', ensureAccountIcon);
    } else {
        ensureAccountIcon();
    }
})();
</script>
                        </ul>
                    </div>
                </div>
            </t>
        </xpath>
        </field>
    </record>

</odoo>
