# This module currently has no custom business logic (Python models).
# This package is scaffolded so future model overrides / extensions
# (e.g. res.config.settings, product.template, sale.order) have a home
# without needing to restructure the module again.
